  # Component: the surface a project declares typed, read from its own
  # `[tool.mypy]` (BDL-068 S4, BDL-UX #231). A component rather than a feature:
  # the capability an adopter runs is the commit gate that consumes it. Derived
  # rather than listed because `beadloom-mr2l.82` listed it, and the mypy
  # configuration then moved while the hook did not.
nodes:
  - ref_id: typed-surface
    kind: component
    summary: "The files a project declares type-checked, and the three sentences a check over them has: not checked, nothing to check, and this many of that many"
    source: src/beadloom/application/typed_surface.py
    docs:
      - docs/domains/application/components/typed-surface/DOC.md
edges:
  - src: typed-surface
    dst: application
    kind: part_of
