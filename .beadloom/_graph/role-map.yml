nodes:
  - ref_id: role-map
    kind: feature
    summary: "role_map_report — every role this flow composes, checked in both directions against the map each DECLARED TOOL's reader opens"
    source: src/beadloom/onboarding/role_map.py
    docs:
      - docs/domains/onboarding/features/role-map/SPEC.md
edges:
  - src: role-map
    dst: onboarding
    kind: part_of
  - src: role-map
    dst: role-composer
    kind: depends_on
  - src: role-map
    dst: flow-composer
    kind: depends_on
  - src: role-map
    dst: flow-config
    kind: depends_on
  - src: role-map
    dst: role-adapters
    kind: depends_on
