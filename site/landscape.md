---
title: Landscape map
---

# Landscape map

Generated by `beadloom docs site` from the `federate` graph — never hand-drawn. Edges are labelled by their cross-repo `ContractVerdict`; node colour reflects health (green = healthy, red = broken, grey = external/expected). Click a service to open its page.

The single-repo landscape (one product).

```mermaid
graph LR
    application[application]
    class application healthy
    click application "/services/application"
    beadloom[beadloom]
    class beadloom healthy
    click beadloom "/services/beadloom"
    cli[cli]
    class cli healthy
    click cli "/services/cli"
    context_oracle[context-oracle]
    class context_oracle healthy
    click context_oracle "/services/context-oracle"
    doc_sync[doc-sync]
    class doc_sync healthy
    click doc_sync "/services/doc-sync"
    graph[graph]
    class graph healthy
    click graph "/services/graph"
    infrastructure[infrastructure]
    class infrastructure healthy
    click infrastructure "/services/infrastructure"
    mcp_server[mcp-server]
    class mcp_server healthy
    click mcp_server "/services/mcp-server"
    onboarding[onboarding]
    class onboarding healthy
    click onboarding "/services/onboarding"
    reindex[reindex]
    class reindex healthy
    click reindex "/services/reindex"
    tui[tui]
    class tui healthy
    click tui "/services/tui"
    application -->|CONFIRMED| context_oracle
    application -->|CONFIRMED| doc_sync
    application -->|CONFIRMED| graph
    application -->|CONFIRMED| infrastructure
    application -->|CONFIRMED| onboarding
    beadloom -->|CONFIRMED| application
    beadloom -->|CONFIRMED| context_oracle
    beadloom -->|CONFIRMED| doc_sync
    beadloom -->|CONFIRMED| graph
    beadloom -->|CONFIRMED| infrastructure
    cli -->|CONFIRMED| application
    cli -->|CONFIRMED| context_oracle
    cli -->|CONFIRMED| doc_sync
    cli -->|CONFIRMED| graph
    cli -->|CONFIRMED| infrastructure
    cli -->|CONFIRMED| onboarding
    context_oracle -->|CONFIRMED| infrastructure
    doc_sync -->|CONFIRMED| infrastructure
    graph -->|CONFIRMED| context_oracle
    graph -->|CONFIRMED| infrastructure
    mcp_server -->|CONFIRMED| application
    mcp_server -->|CONFIRMED| context_oracle
    mcp_server -->|CONFIRMED| doc_sync
    mcp_server -->|CONFIRMED| graph
    mcp_server -->|CONFIRMED| infrastructure
    mcp_server -->|CONFIRMED| onboarding
    reindex -->|CONFIRMED| context_oracle
    reindex -->|CONFIRMED| doc_sync
    reindex -->|CONFIRMED| graph
    classDef healthy fill:#d4edda,stroke:#28a745,color:#155724;
    classDef broken fill:#f8d7da,stroke:#dc3545,color:#721c24;
    classDef neutral fill:#e2e3e5,stroke:#6c757d,color:#383d41;
```

