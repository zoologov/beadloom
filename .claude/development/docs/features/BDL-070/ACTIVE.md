# ACTIVE: BDL-070 — The layer a node is in, answered once and reported over its population

> **Last updated:** 2026-09-12
> **Phase:** Development

---

## Current Bead

**Bead:** A1 `beadloom-e64o` — the shared layer lookup, pure and declaration-reading
**Goal:** one `layer_of` reading the rule's declared layers and climbing `part_of`, plus the
population counter everything else reports from.
**Done when:** `layer_of` is pure, a `part_of` cycle terminates, a node with its own tag does not
climb, exactly one ancestry walk is reachable from `graph/rules/`, and no layer tag appears as a
literal in `src/` outside a test fixture.

## Progress

- [x] Step 0: docs folder created (2026-09-12)
- [x] Step 0.5: Explore run; `## Axes` derived, and its supplement derived by hand after the
      derivation reported no seed (2026-09-12)
- [x] PRD approved (2026-09-12)
- [x] RFC approved, with Q1–Q6 decided or deferred to a named bead (2026-09-12)
- [x] CONTEXT + PLAN approved (2026-09-12)
- [x] Beads created: epic `beadloom-5tcc` + 16, one plan, 21 edges confirmed against the titles
- [ ] Wave 1: A1
- [ ] Waves 2–6: Release A
- [ ] Waves 7–12: Release B

## Results

> The bead id is the FIRST cell of every row on purpose: the focus-document medium reads
> the first cell only, and a table carrying the id in its second column reports every bead
> of the wave as having no row (BDL-UX #272).

| Tracker | Bead | Status | Details |
|---|---|---|---|
| `beadloom-e64o` | A1 | Done    | `graph/rules/layers.py`: `layer_of` reads the declared `layers`, `part_of_generations` is now the ONE ancestry walk (`import_resolver` calls it), `layer_population` counts evaluated / skipped-untagged. 31 + 5 tests. No verdict moved: lint 0 error(s), 70 warning(s), as before |
| `beadloom-1ylk` | A2 | Done    | `evaluate_layer_rules` states its reach: `layer_reach.py` emits one `layer_population` finding per rule (`warn`, never the declared `error`), `node_tags.py` replaces the five identical tag closures. Measured here: 16 of 363 by own tags, 355 of 363 by `part_of`. 30 + 3 tests. No verdict moved — `lint --strict` exit 0 before and after, 0 findings removed, 1 added, and the identity is asserted against the pre-change code path run in the same process |
| `beadloom-2dgz` | A3 | Done    | `LintResult.layer_populations` carries one `LayerReach` per declared layer rule, counted in `_evaluate` beside `inert_rule_names` from the same `reach_of` the evaluator uses. All four renderings state it: a clause on the rich summary line, GREEN and RED alike; `summary.layer_populations[]` in `--format json`, additive — the ten keys that were there are asserted by name; a leading `::notice::` in `--format github`; a leading `# layer_population:...` line in porcelain, behind the `# ` marker `scope-check` already uses. `beadloom lint`'s clean line carries the clause too, and its condition moved from "the formatter printed nothing" to "the format states no verdict of its own" — keyed on emptiness it would have vanished from the two formats it exists for. The clause is present at FULL reach where the finding is silent, because a clause on a line already being read costs nothing and a finding is triaged in every project. 23 new tests. Measured with the index lineage held constant — one fresh index in `room-beadloom-2dgz`, read by both sides with `--no-reindex`: 0 findings removed, 0 added, exit 0 both sides; the one added line is the marked population line, which is not a finding |
| `beadloom-q6jh` | A4 | Done    | All five surfaces state the population, asserted per surface: `gate.py` `lint_step` appends `_population_note` beside `_suppressed_note`; `handle_lint`'s summary carries `layer_populations[]`, outside the severity filter because a finding filter must not hide something that is not a finding; the TUI provider carries the finding's `rule_type` and `message`, which it dropped, and the panel leads with the population row rendering its message — it printed `architecture-layers (None) - <the rule's description>` before; the debt report counts the reaches over the rules it already loaded and carries the clauses to `DebtReport`, the Rich report and the JSON; `prime` states them on the `Health:` line and under `health.layer_populations`, one clause per RULE so the bounded finding list can grow without it. One wording, not five: `layer_reach.population_phrase` and `stated_populations`, which `linter._population_note` and `format_github` were rewritten to call. The caller set is derived by AST over the installed package and compared for equality — `linter._evaluate`, `data_providers.refresh`, `collect._count_violations` — and confirmed to fail on a set with one entry removed; its ceiling (an attribute call binds no name it reads) is held by a case of its own. 29 new tests. Measured with the index lineage held constant, one fresh index in `room-beadloom-q6jh` read by both sides with `--no-reindex`, each side running its own sources: `lint --strict --format porcelain` identical line for line, 72 both sides, 0 removed, 0 added, exit 0 both sides. Per surface: the Gate keeps `0 error(s), 71 warning(s)` and `passed: True`, `prime` keeps `71 lint violations` and grows 4588 → 4650 bytes, the MCP summary's three keys are unchanged, the TUI row count stays 71. Two defects found by the measurement and FILED rather than fixed, because both move a number in a release that ships none: #291 / `beadloom-is2z` (the debt report resolves `rules.yml` from two paths the product does not write, so its rule-violation category is 0 against 71 here) and #292 / `beadloom-vu0a` (the lint panel branches on `severity == "warning"` against a vocabulary of `error`/`warn`, so its warning count is always 0) |
| `beadloom-06dz` | A5 | Done    | `architecture_view` drops `_LAYER_TAGS`, `_LAYER_RANK`, `_layer_of`, `_own_layers` and `_layer_rank`, reads the declared layer order from the indexed `rules` table and resolves membership through `graph.rules.layers`; `liveness` reads its layer through `own_layer_of` and its tags through `node_tags`, keeping OWN-tag membership so no inert verdict moves. `src/` now holds no layer tag as a literal at all — the guard's exemption set is empty. 10 new tests; 3 existing test files adjusted. Measured here: the artifact is byte-identical over 106 nodes (136 743 bytes), and `lint --strict --format porcelain` is identical line for line after a full reindex — 0 findings removed, 0 added, exit 0 both sides. The view's `dst_rank <= src_rank` predicate is untouched (B4) |
| `beadloom-punn` | A6 | Done    | Q5 answered in the bead's comments before any code change, with the re-derivation: the `tags:` catalog is REMOVED, and `load_rules_with_tags` with it — rules.yml declares constraints ON the graph while a node's tags are a property OF it, and an AST scan of every module under `src/` (imports including asnames, calls, attribute calls) found no production reader, so the trap goes rather than the block alone. `validate_rules` gains the `LayerRule` case through `layer_declaration.py`, whose one predicate also feeds the evaluator's `warn` finding — `warn`, never the declared `error` — and stands down when fewer than two layers are populated, because liveness names them there. `rules.yml` has an owner: the `architecture-rules` node, and `beadloom impact .beadloom/_graph/rules.yml` names it owning 1 unread file. 18 new tests. Measured with the index lineage held constant: 71 findings before and after, 0 removed, 0 added, exit 0 both sides; one advisory message moves, `scenario-coverage`'s population 106 → 107 nodes, because this bead adds one. Nothing on this repository triggers the new finding — all four declared tags are carried |
| `beadloom-cfkk` | A7 | Done    | The six scenarios the PRD references by name landed as `layer_population.feature` and `layer_declaration.feature`, bound to steps that build a real project directory, index it with the real reindex and read it through the real linter. Their layering is `tier-web` / `tier-core` / `tier-store`, a vocabulary `src/` does not contain, because a scenario written over `layer-domain` passes against an implementation that hardcoded it. Each was shown red before being reported green: disabling inheritance in `layer_of` fails the agreement scenario, silencing `population_statement` fails two more and two neutrality tests, and emptying `declaration_warnings` fails the `validate_rules` one. Neutrality on a project that is NOT this repository is now a test rather than a shell measurement — `test_release_a_is_neutral_on_a_project_that_is_not_ours.py`, 9 tests over the whole `lint()` run, with the pre-change path transcribed ONCE in `tests/the_lint_path_before_release_a.py` and A2's module reading it from there. **Two measurements on this repository, the index lineage held constant as a FRESH index built in each worktree and read with `--no-reindex`, each side running its own sources.** Pre-Gherkin, `49e6497a` -> `43286775`, the whole of Release A: 70 -> 71 findings, 0 removed, 1 added (`architecture-layers:layer_population:warn`), errors 0 -> 0, exit 0 both sides; at message level one further line moves, `scenario-coverage`'s denominator 106 -> 107 graph nodes, which is an advisory rather than a decision. Post-Gherkin, `43286775` -> this bead: 71 -> 61 findings, 0 added and 10 removed, every one of the ten a `scenario-coverage` reference finding on `PRD.md` — the six names going from `not in the suite` to present — and the suite's population phrase moving `483 scenarios in 71 files` -> `489 scenarios in 73 files` in 59 messages. Errors 0 and exit 0 at all three commits. `layers.py` needed no new mutation TARGET: `mutation.targets` declares the directory `src/beadloom/graph/rules/` and `[tool.mutmut] only_mutate` matches it with `src/beadloom/graph/rules/*`. What was missing was the killers, so the three new test files joined `pytest_add_cli_args_test_selection` after each was checked for the BDL-UX #289 shape and none derives a path from `__file__`. Coverage of the modules Release A wrote, measured in `room-beadloom-cfkk`: `layer_reach.py` and `layer_declaration.py` 100%, `layers.py` 99%, `node_tags.py` 95%, `architecture_view.py` 96%, `loader.py` 84%; 30 816 statements 95% overall. One defect found and fixed here rather than filed: the steps modules first imported their project builder from `tests/`, which `test_a_step_the_suite_no_longer_implements_reddens_the_run` caught — it copies `tests/acceptance/` out and runs it, so a suite that reaches outside itself fails at collection instead of on the binding it was sabotaging. The builder now lives in `tests/acceptance/steps/tiered_project.py` |
| `beadloom-mrof` | A8 | Done    | review, release A |
| `beadloom-5tcc.1` | A8-fix | Done | Major 1: `graph/rules/advisories.py` — `ADVISORY_RULE_TYPES` (`layer_population`, `layer_declaration`) with the reason at the set, read by the new `LintResult.fails_on_warn`, which `lint --fail-on-warn` exits on instead of `result.violations`. Asserted in BOTH directions in the neutrality file: the reviewer's fixture (2 tiered, 1 untiered) exits 0, and a project whose layering is declared `warn` and broken still exits 1. Major 2: `node_tags._read_all_tags` skips a row whose `extra` does not parse — three-row fixture (valid / NULL / malformed) plus an `evaluate_layer_rules` case that used to raise a `JSONDecodeError` through `evaluate_all`. Major 3: `_layer_view` logs at INFO when the index holds no layer rule while `layer-*` tagged nodes exist, with the count; the consequence is written in the site-generation SPEC and the application README, and A9's done-when now carries the release-note line verbatim. Minors 4-6: `ci-setup.md` porcelain line and a `--fail-on-warn` paragraph, `as_mapping()` hoisted above the layer loop, and the twice-read containment/tag pair stated at both ends. 9 new tests + 1 scenario (`@bead:beadloom-5tcc.1`, shown red by restoring the old condition). **Verdict-neutral on this repository, index lineage held constant** — one index, this tree's, read with `--no-reindex` by HEAD's sources (88e4315a, a worktree) and by these: porcelain identical, 62 lines both; `--format json` identical but for `elapsed_ms`; `--fail-on-warn` exits 1 on both sides here, because this repository's `scenario-coverage` warnings already decide it. Green in a clean room over 21 files (`room-beadloom-5tcc.1`, Darwin arm64 / CPython 3.13.7 / utf-8, 0 of 21 declared rooms entered): 10506 passed, 62 skipped, 13 xfailed, `mypy --strict` and `ruff` clean. `beadloom ci` on the tree: rc 0 |
| `beadloom-5tcc.2` | A8-rr | Done    | re-review after the fix cycle |
| `beadloom-5tcc.3` | A8-fix2 | Done | Major: `node_tags._read_all_tags`'s docstring claimed `NodeTags.of` and `loader.get_node_tags` answer alike for every malformed `extra`, and contradicted itself two paragraphs later. The four shapes were MEASURED here rather than transcribed — `TestWhereTheTwoTagReadersAgreeAndWhereTheyDoNot`, 9 cases, one stored shape at a time: they agree on SQL `NULL`, an object with no `tags` key, an empty `tags` list, a `tags` list and a node the graph does not hold; they diverge on `null` / `3` / `"x"` (`set()` vs `AttributeError`) and on `{not json` (`set()` vs `json.JSONDecodeError`). The docstring now carries that table and points at the test, and both guards are pinned per shape: dropping the `isinstance` guard reddens exactly the three `AttributeError` rows, dropping the `json.JSONDecodeError` guard exactly the fourth. Minor 2: `project_declaring_a_tier_no_node_is_in` (three tiers declared, two populated, every edge judged) and a fourth case in `TestTheExitCodeFailOnWarnReads` — with `LAYER_DECLARATION_RULE_TYPE` removed from the frozenset that file goes 1 failed / 12 passed, and the one failure is the new case, where the re-review measured 421 tests staying green. Minor 3: `fails_on_warn` is `any(v.severity == "error" or not is_advisory(v) ...)`, the coordinator's direction as stated, so the flag stays a superset of `--strict` instead of resting on both advisory constructors hardcoding `warn`; shown red first (3 failed / 5 passed). The four documents that state the exclusion say where it stops. 18 new test cases, no new scenario: the severity bound is unreachable from a project directory today, because nothing emits an advisory at `error`. **Verdict-neutral on this repository, index lineage held constant** — one index, this tree's, as the Gate's reindex left it, read with `--no-reindex` by HEAD's sources (`63ed703f`, a worktree) and by these, through one interpreter: `--strict --format porcelain` identical line for line, 62 both sides, 0 removed and 0 added; exit codes 0 / 0 / 1 on both sides for no flag, `--strict` and `--fail-on-warn`, the last decided here by this repository's `scenario-coverage` warnings; `--format json` identical but for `elapsed_ms`. Green in a clean room over 10 files (`room-beadloom-5tcc.3`, Darwin arm64 / CPython 3.13.7 / utf-8, extras dev+graphql+languages+mutation+tui+watch, 0 of 21 declared rooms entered): 10525 passed, 61 skipped, 13 xfailed in 508.58s, `mypy --strict` and `ruff check` clean. That room cannot see a bead running beside it, and the combined tree is the wave gate owner's measurement. `beadloom ci` on the tree: rc 0, 0 `::error`, 211 `::warning`, none about this change; `mypy --strict src/` clean against all four declared target versions (3.10-3.13), one interpreter |
| `beadloom-5tcc.4` | A8-rr3 | Done    | review pass 3 |
| `beadloom-714v` | A9 | Done    | **Release A documented as a REPORT, and the sentence that says what it is not.** `rule-engine/SPEC.md` gains `##### What the rule reports, and what it still decides on`: `evaluate_layer_rules` resolves both ends through `own_layer_of` and moves on when either end declares no tag, so **inheritance has NOT shipped** and `beadloom-ku26` is named as the bead that moves it; the population is a measurement taken here rather than a figure carried forward — 365 live `depends_on`, **16 judged** by own tags, 349 skipped, **357** by `part_of` ancestry, 2026-09-13 over a warm full rebuild, porcelain record `16:365:349:357` — and it points at the BDL-UX #290 lineage note, which is why the 362 and 363 quoted elsewhere in the same section are not contradictions; the third bullet states what B2 did NOT do, that a same-layer edge is still legal unconditionally and the 14 `exempt:` entries (14 crossings, 14 excused, 0 unexcused, 0 dead, measured through `excused_crossings`) are consulted only to report a DEAD or EXPIRED one. `docs/services/cli.md` carried the `--fail-on-warn` exclusion and NOT the output the release added: all four `--format` bullets now state the population, and a new paragraph carries the sample line, the deliberate asymmetry (clause at full reach, finding silent) and the same "report, not a verdict change" sentence. **The four widened done-whens.** (1) The recorded surface was the false green: `sync-check` rc 0 was already true while `.beadloom/sync-surface.json` said 473. Re-recorded and committed — 473 → **489 pair(s)**, 107 → **108 doc(s)** — and the `declared surface grew` line is gone, with **0 stale pairs**. (2) The lane case is in the release note verbatim, in `CHANGELOG.md`'s upgrade-note block with the remedy beside it; it was in the site-generation SPEC and the application README already and in no release note. (3) The `--fail-on-warn` bound: `docs/domains/graph/README.md` now says the exclusion stops at `error`. **The other two copies are in `src/` and this role edits only `docs/`** — filed as `beadloom-5tcc.5` with both line ranges, both exact strings and the reason neither is false today. (4) Both unwrapped lines rewrapped: `ci-setup.md` 143 → 82 columns, the SPEC 117 → 95. **One gap `sync-check` reads as `ok`**, which is the class the role protocol warns about: `layer_exemptions.py` is in the SPEC's module list and was missing from the graph README's `rules/` bullet — B2 re-indexed after writing it, so no pair was stale. Added. **One Gate error introduced and closed inside this bead**: the sample clause in prose put `16` inside the proximity window of `edge`, so `docs audit` read it as an `edge_count` claim of 16 against 601 and `beadloom ci` went red; the sample moved into a fenced block, which the scanner skips. `CHANGELOG.md` gains a BDL-070 Release A intro, the upgrade-note paragraph, three `Added` bullets, one `Changed` bullet and one `Removed` bullet. **Measured on the tree**: `beadloom ci` rc 0, 0 `::error`, 212 `::warning`, and the warning set is identical line for line to the run taken before these edits; `docs audit` rc 0, no stale mentions; `docs quality` rc 0 over 269 documents; `sync-check` rc 0. **Green in a clean room over 6 files** (`room-beadloom-714v`, from `48e54e29`, Darwin arm64 / CPython 3.13.7 / utf-8, extras dev+graphql+languages+mutation+tui+watch, 0 of 21 declared rooms entered): 10577 passed, 61 skipped, 13 xfailed in 498.72s. The first run of that room reported 2 failed — `TestSelfLint::test_self_lint_clean` and `_strict`, both `sqlite3.OperationalError('disk I/O error')` — and both passed on an immediate re-run in the same room and in the full second run, so it is recorded as an environment fault of the room's filesystem and not as a difference in the sources; no source file is changed by this bead. That room cannot see a bead running beside it, and the combined tree is the wave gate owner's measurement. **Five documents keep their surface drift and are deliberately NOT attested** — `README.md`, `README.ru.md`, `docs/architecture.md`, `docs/guides/bdd-scenarios.md`, `docs/guides/parallel-waves.md`: an attestation is the claim that somebody read the document against the new surface, and this bead read only `docs/services/cli.md`, which is attested |
| `beadloom-46am` | B1 | Done    | **A real layering violation, decided and measured before any code changed.** The edge has exactly two sources, both real: `onboarding/scanner/init_flow.py` lines 144 and 319, `from beadloom.application.reindex import reindex`, function-local and indexed as such. An AST pass over every module of the six domain packages finds those two and nothing else importing `beadloom.application` — fourteen other mentions are Sphinx cross-references, which the resolver correctly does not make edges of. Under inheritance it is the ONLY reverse-direction edge here: 364 live `depends_on`, 356 judged, 1 reverse, 16 same-layer crossings (B2's) and 8 skipped, all eight into the untagged root service. Not an exception either, and not by preference: `LayerRule` (`graph/rules/types.py:261`) carries no exemption field, so recording one would mean adding an `exempt:` clause to the rule B3 exists to build, in order to excuse the one edge it would catch. **Fixed by dependency inversion.** `scanner/reindex_port.py` states what onboarding needs — `IndexCounts`, four read-only counts, and `Reindexer` — and `services/commands/setup.py`, a service above both layers, supplies `application.reindex.reindex`. Required rather than defaulted, because every default keeps the violation or hides it: a `None` meaning "do not re-index" is the BDL-067 `.14` defect with a new cause, a lazy import is the violation unchanged, and `importlib.import_module` removes the edge by hiding it from the scanner, which is the BDL-059 S3 workaround the guard test exists to prevent. The call did not move; only where the callable comes from did. 22 new test cases across 3 files, each shown red first: the guard that found it is now general and reads the DECLARATION — layer order from `rules.yml`, membership from each node's own `tags:`/`source:` — so it names no tag and no package and covers every layer, not the one pair BDL-059 S3 wrote it for. **One existing instrument went red and was repaired rather than relaxed**: `test_every_caller_of_the_skeleton_writer.py` derives "does this caller re-index" from IMPORT bindings, so an injected callable made both init entry points read as re-indexing nowhere; it now also reads a parameter whose annotation the file imported from the port, deriving that too rather than naming a type. **Measured with the index lineage held constant as a WARM full rebuild on each side** — `reindex --full` over an existing index in each worktree, read with `--no-reindex`, each side running its own sources: `lint --strict --format porcelain` 62 lines both sides, 0 findings removed and 0 added, exit 0 both sides, `--fail-on-warn` exit 1 both sides on this repository's pre-existing `scenario-coverage` warnings. **One line moves and it is not a finding:** the population line `16:365:349:357` → `16:364:348:356`, one edge fewer because this bead removed one. The Gate agrees step for step, `16 of 365` → `16 of 364`, `beadloom ci` rc 0 and 0 `::error` on both sides. **The lineage discipline earned itself here:** the first comparison put a COLD before-index against a warm after-index and read the two as identical, 62 = 62, because BDL-UX #290's lineage difference added an edge on one side while this bead removed one — a mismatched lineage does not only add noise, it can subtract a real change. **#290 itself was answered and NOT fixed here**, per this bead's done-when: no new number was opened, because the defect is already filed. Both of its unestablished points are now measured and appended to the entry and to `beadloom-xzvp` — it is the FIRST build of an empty index against every build after it, not fresh-worktree against carried-forward (same worktree, no code difference, three consecutive `reindex --full`: 364 / `application`, then 365 / `graph-reads`, then 365); the class is exactly 2 of 2371 `code_imports` rows, both importing `beadloom.application.graph_reads`; the warm answer is the correct one and the cold index contradicts itself, because Strategy 1 of `import_resolver.py:809`-`:820` needs the imported file to be in `code_symbols` or `file_index` and both are empty while `index_imports` runs on a first build. Green in a clean room over 12 files (`room-beadloom-46am`, from `30352b92`, Darwin arm64 / CPython 3.13.7 / utf-8, extras dev+graphql+languages+mutation+tui+watch, 0 of 21 declared rooms entered): 10538 passed, 61 skipped, 13 xfailed in 513.51s, `mypy --strict` and `ruff check` clean. The room carries no rename, so the file this bead renamed away was deleted inside it by hand. That room cannot see a bead running beside it, and the combined tree is the wave gate owner's measurement. `beadloom ci` on the tree: rc 0, 0 `::error`, 211 `::warning`, none about this change; `mypy --strict src/` clean against all four declared target versions (3.10-3.13), one interpreter |
| `beadloom-xmfs` | B2 | Done    | **Seventeen dispositioned, and the seventeenth was not a crossing.** The coordinator's 17 was taken under a predicate one notch stricter than the one RFC Q1 decided, and the two disagree on exactly one edge: `cli-commands -> guard-probes`. Both ends are `part_of cli`, which carries `layer-service`, so under "both ends share a tagged ancestor" it is internal and under "same NEAREST tagged container" it is a crossing. Reflexive shared-ancestor is the reading B3 should ship, or a component will cross with the container it is inside. Under it, measured on this tree over a warm full rebuild: 364 live `depends_on`, 356 layered by ancestry, 132 same-layer, **16 crossings**, not 17. **Two FIXED, fourteen EXEMPTED.** Every one of the sixteen is import-derived — no stale hand-declared edge to delete — so each was read at its import site before it was dispositioned. The fix: `exit_condition_deadline` was written inside `graph/rules/types.py` and its own docstring names two surfaces that require an `until:`, one of them `flow.yml`'s, which `onboarding` reads — so `config-check -> rule-engine` and `flow-suppression -> rule-engine` were one peer-domain import of a shared vocabulary. It moved to `infrastructure/exit_condition.py`, below both declarers, beside `scan-paths` and `doc-roots` for their reason; `beadloom.graph.rules.exit_condition_deadline` still answers, by re-export. **That trade is not free and is not reported as free:** `onboarding-no-direct-infra` caught the new import and it is baselined as that rule's fifth entry, whose own declaration says it is over-broad ("re-scoped to what it actually means — open no index, run no git") and whose `node_source` entry is the identical shape. The finding `architecture-layers` makes is removed; the one this rule makes is excused, in those words, in the entry. **A bare allow is refused by the loader, not asked for.** `exempt:` on a layer rule parses into `LayerExemption(from_glob, to_glob, reason, until)`; an entry omitting any of the four, or matching every edge with `*`/`*`, is a rules-load `ValueError`. Matching is by `ref_id` on BOTH ends and by direction — excusing `a -> b` does not excuse `b -> a`, asserted. **The declaration is not inert in the release that creates it.** `evaluate_layer_rules` computes the crossings and reports every entry that is DEAD (excuses nothing) or EXPIRED (past its own deadline while still excusing), at `warn`, verdict-neutral: what B3 adds is the finding for an un-excused crossing, not the bookkeeping. An exemption nothing consults is one nobody can tell is dead, which is the failure `exemptions.py` exists to prevent. **One measured non-answer, recorded in `rules.yml` so nobody re-derives it:** nine of the sixteen are `onboarding -> graph` / `-> context-oracle`, which reads like a mis-declared layer, since onboarding orchestrates bootstrap. Re-tagging it `layer-application` makes it worse — the nine go away and twenty-two appear, 16 -> 29, because the application layer depends on onboarding in twenty-two places. Two other consolidations are named with their counts instead of started: four crossings are one dependency (three domains reach `code-indexer` for tree-sitter symbol extraction), and three are one `prime` lint call that wants B1's port shape. **Re-measured after the work, not assumed:** 365 live `depends_on`, 357 layered, 130 same-layer, **14 crossings**; 14 of 14 excused by a named entry, 0 left unexcused, 0 entries excusing nothing. So `lint --strict` is rc 0 on this repository the day B3 ships, which is B3's own done-when. 22 new test cases across 3 files plus 6 scenarios in `layer_exemptions.feature`, each shown red first; the fixture graphs declare `tier-web`/`tier-core`/`tier-store`, a vocabulary this repository does not ship. Two existing instruments went red and were repaired rather than relaxed: the `rule_engine` shim's identity re-export and the mutation pool, which had three test files importing a declared target from outside it. **Index lineage held constant as a WARM full rebuild on each side** — `reindex --full` over an existing index before and after, the lineage B1 used, because BDL-UX #290's first-build-of-an-empty-index resolves 2 of 2371 import rows differently. Green in a clean room over 26 files (`room-beadloom-xmfs`, from `4130253a`, Darwin arm64 / CPython 3.13.7 / utf-8, extras dev+graphql+languages+mutation+tui+watch, 0 of 21 declared rooms entered): 10576 passed, 62 skipped, 13 xfailed in 512.45s, `mypy --strict` and `ruff check` clean. That room cannot see a bead running beside it, and the combined tree is the wave gate owner's measurement. `beadloom ci` on the tree: rc 0, 0 `::error`, 211 `::warning`, none about this change; `sync-check` rc 0; `mypy --strict src/` clean against all four declared target versions (3.10-3.13), one interpreter. |
| `beadloom-5tcc.5` | A9-fix | Done | **Both `src/` copies now carry the bound, and they carry it in the same words.** The choice was to add the half-sentence rather than point at `graph/rules/advisories.py`, on a measurement rather than a preference: click renders BOTH copies into `beadloom lint --help` — the option's `help=` and the command docstring — so a reader of either has no source tree in hand and a path into `src/` is not something they can open. The clause is the one `docs/domains/graph/README.md` already carries, minus its backticks, since neither surface is RST-rendered: `An advisory emitted at error severity exits 1 under --fail-on-warn as it does under --strict.` The sentence before it was tightened to `advisory and do not exit 1 at warn severity` / `advisory and exit 0 at warn severity`, so the two read as one statement instead of contradicting each other. **The bead's premise was re-measured, not carried:** `LintResult.fails_on_warn` is `any(v.severity == "error" or not is_advisory(v) for v in self.violations)` (`graph/linter.py:142`), and each advisory module constructs exactly ONE `Violation`, both hardcoding `severity="warn"` (`layer_reach.py:232`, `layer_declaration.py:111`) — so neither copy was false, and the bound is for the day one of them is emitted otherwise. **The test SWEEPS rather than naming the two copies**, because a third is added the way the second was: every help text the `lint` command renders that calls a finding advisory must carry `BOUND_CLAUSE` verbatim, and a population test asserts the sweep reaches exactly `docstring` and `--fail-on-warn` — a sweep over an empty set is green and useless. Scoped to `lint` deliberately: `advisory` is used elsewhere in this CLI in an unrelated sense (`setup.py:423`, `advisory-strong`), so a CLI-wide sweep on the word fires on text this rule has nothing to say about. 7 new test cases, shown red first — both copies failed, with the assertion naming the clause to add. **Verdict-neutral on this repository:** `beadloom lint` output identical line for line before and after (`diff` rc 0), `lint --strict` rc 0, Gate `lint PASS: 0 error(s), 61 warning(s), architecture-layers judged 16 of 365`. Four `sync-check` pairs went stale on `federation.py` by hash alone — `application`, `context-oracle`, `graph`, `cli-commands` — and each was attested per pair with `--pair`/`--code` after reading its lint prose, none of which states the exclusion; `docs/services/components/cli-commands/DOC.md` holds no fourth copy (`grep` for `advisor` finds none). `ruff format` was NOT run over `federation.py`: it is already unclean at `HEAD` in a region this bead does not touch (lines 579-663), and format is in neither CI nor the Gate. **Green in a clean room over 2 files** (`room-beadloom-5tcc.5`, from `fea2e3f4`, Darwin arm64 / CPython 3.13.7 / utf-8, extras dev+graphql+languages+mutation+tui+watch, 0 of 21 declared rooms entered): 10583 passed, 62 skipped, 13 xfailed in 510.96s, `mypy --strict` and `ruff check` clean, and clean against all four declared target versions (3.10-3.13) under one interpreter. That room cannot see a bead running beside it, and the combined tree is the wave gate owner's measurement. **On the tree:** `beadloom ci` rc 0, 0 `::error`; full suite 10632 passed, 13 skipped, 13 xfailed in 496.45s |
| `beadloom-ku26` | B3 | Ready | derived layer + predicate |
| `beadloom-w34m` | B4 | Blocked | architecture_view's predicate |
| `beadloom-bi78` | B5 | Blocked | tests, release B |
| `beadloom-ssz8` | B6 | Blocked | review, release B |
| `beadloom-57wl` | B7 | Blocked | the shipped claim |

## Notes

- **The axes derivation found no seed** and said so. The scope decision rests on a supplement
  derived by name-matching, which is weaker, and A8's done-when requires the reader set to be
  re-derived by a different method.
- **Q5 and Q6 were closed against the `pending-in-approved` check**, not left open: Q6 is decided
  (both the finding and the summary line, for a derived reason), Q5 is deferred to A6 with the
  answer required in writing before code changes.
- `beadloom docs quality` reports the `Waves` table in PLAN.md as NOT CLASSIFIED — the tool stating
  it does not read that table, not a finding against the document.
