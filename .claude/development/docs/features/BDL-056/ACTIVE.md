# ACTIVE: BDL-056 — Positioning refresh + writing-quality standard + docs hygiene

> **Type:** chore
> **Branch:** features/BDL-056
> **Parent bead:** beadloom-7gxi
> **Updated:** 2026-06-15

---

## Current focus

**ALL BEADS DONE — branch ready for one PR to `main`.** `.3 [review]` PASS on all 9 acceptance criteria (no findings; review subagent re-ran `beadloom ci` rc 0, 4228 tests, clean VitePress rebuild). `.2 [tech-writer]` DONE: README en/ru rewritten to the locked thesis (RU owner-validated over several iterations: federation framing sharpened vs Hive, the four pain bullets collapsed, dedup pass, terminology fixes); EN translated 1:1. The agent-architecture guide now ships bilingual (`multi-agent-development.md` EN + `.ru.md`) with an in-page cross-link like the About toggle, `.ru.md` excluded from the sidebar (site_nav.py + test). Generated `site/` content removed from git (owner's call) — gitignored, regenerated by CI/deploy; only the hand-authored shell (config.mjs, theme/, package*) stays tracked; orphan root `site/dashboard.data.json` deleted (served copy is the CI-generated `site/public/dashboard.data.json`). P0 `beadloom-mft7` no longer reproduces (no `{{ }}` left; site build green).

## Bead status

| Bead | Role | Status | Depends |
|------|------|--------|---------|
| beadloom-7gxi.1 | dev | ✓ done | — |
| beadloom-7gxi.2 | tech-writer | in_progress | .1 |
| beadloom-7gxi.3 | review | ready | .2 |

## Plan notes

- README (en/ru) is authored **interactively in the main loop**, RU-first: draft `README.ru.md` → owner validates → iterate → after RU approval translate to `README.md` (en ≡ ru). Not a fire-and-forget subagent.
- `docs/vision.md` was dropped (owner's call 2026-06-15): the thesis becomes the README lead; one less prose doc to keep fresh, no duplication/drift.
- Agent-architecture doc lands at `docs/guides/multi-agent-development.md`; its 2.0.0 rewrite is already done and sits uncommitted in the working tree — the `git mv` must preserve that content.

## Watch-out

- Pre-existing **P0 bug beadloom-mft7**: VitePress build broken by `{{ }}` Vue-interpolation in `docs/guides/ai-techwriter.md`. This blocks the "site build green" acceptance criterion for `.2`/`.3`. Decide with owner whether to fold its fix into this branch or land it first.

## Progress log

- 2026-06-15 — BRIEF approved; parent + 3 sub-beads created (beadloom-7gxi.1/.2/.3) with deps; branch `features/BDL-056` created (carried the uncommitted agent-arch doc). Starting `.1`.
- 2026-06-15 — `.2 [tech-writer]` DONE: README.md + README.ru.md rewritten to the locked data-core thesis (RU-first, owner-validated across several rounds; federation framing reworked to be unique vs Hive — multi-transport + intent/lifecycle + declared deps; pain section trimmed; dedup pass; "Единый Gate" / "Поверх графа" wording fixed); EN translated 1:1. Agent-architecture guide shipped bilingual (`multi-agent-development.md` EN primary + `.ru.md`) with About-style in-page cross-link; `.ru.md` kept out of the sidebar (site_nav.py `.ru.md` filter + test). **Generated `site/` content un-tracked + gitignored** (owner's call) — regenerated by `beadloom docs site` in site-build + deploy-site; only the hand-authored shell stays; orphan root `site/dashboard.data.json` removed (served copy = CI-generated `site/public/dashboard.data.json`). `beadloom ci` green, VitePress build green (fresh-checkout simulation), 4228 tests pass. P0 `beadloom-mft7` no longer reproduces.
- 2026-06-15 — `.1 [dev]` DONE: standard in CORE role + recomposed/re-vendored tech-writer adapter (drift-guard green); deck source + `.gitignore` entry removed; doc moved to `docs/guides/multi-agent-development.md` (2.0.0 content preserved). 4227 tests pass, ruff clean, `beadloom ci` green. **Correction:** the deck was NOT committed in source (gitignored) but its GENERATED pages live in `site/docs/presentations/` (committed → on the live site); `.2`'s site regeneration drops them. **Lesson:** `setup-agentic-flow --force` inside the Beadloom repo corrupts the vendored `CLAUDE.md.txt` placeholder — reverted; recompose touched only the tech-writer role.
