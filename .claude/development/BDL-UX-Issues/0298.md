# 298

**Allocated:** 2026-09-13
**Holder:** beadloom-jorg

> Allocated by `beadloom issue-number allocate`. The entry's text lives in the
> issue log until the log becomes a composed view of this directory; until then
> this file is the claim on the number and nothing else.

> **Holder repaired by hand, 2026-09-13.** `allocate` ran with `--holder ""` and accepted it, so
> this claim first named nobody. The number was allocated correctly; only this field was empty.
> There is no command to set a claim's holder, so the field was edited. See the entry that
> records the empty-holder defect.
