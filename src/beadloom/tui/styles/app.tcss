/* Beadloom TUI — App-level Stylesheet */

Screen {
    layout: vertical;
}

/* Screen tab bar (future: persistent navigation) */
#screen-tabs {
    height: 1;
    background: $surface;
    color: $text-muted;
    padding: 0 1;
}
